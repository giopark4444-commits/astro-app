# Créditos + Suscripción — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sistema de créditos con débito atómico para el modelo premium (Claude) + refill mensual del plan Plus + venta de packs por Dodo, sin que el costo de API pueda superar jamás el saldo pagado.

**Architecture:** Ledger append-only en Supabase como fuente de verdad del saldo, con funciones SQL `security definer` (solo `service_role`) para gasto atómico y abono idempotente. Las rutas API gastan ANTES de llamar a Claude y reembolsan en fallo total. UI: toggle ✨ en el chat + sección Créditos en Ajustes.

**Tech Stack:** Next.js 15 App Router, Supabase (Postgres + RLS), Dodo Payments (`dodopayments` SDK + `standardwebhooks`), vitest, next-intl.

**Spec:** `docs/superpowers/specs/2026-07-23-creditos-design.md` (leerlo ante cualquier duda de comportamiento).

## Global Constraints

- **Anti-quemada**: NINGÚN camino llama al proveedor premium sin haber descontado crédito antes con éxito (`spend_credits` atómico). Sin `SUPABASE_SERVICE_ROLE_KEY` o sin `ANTHROPIC_API_KEY` → lo premium se degrada al camino gratis de siempre (fail-closed para gastar plata, jamás 500).
- **Idempotencia por `ref`**: índice único parcial sobre `credit_ledger.ref`; abonos con ref namespaced (`dodo:<payment_id>`, `refill:<sub_id>:<period>`, `refund:<spend-ref>`); reintentos del webhook no duplican.
- **`ALUNA_ALL_ACCESS` NO regala Claude**: el gasto de créditos es independiente de `allAccessEnabled()`. El tope diario gratis SÍ se apaga con all-access activo (hoy la app está abierta).
- **Reembolso solo en fallo total**: si el stream premium emitió 0 caracteres y falló → refund; con salida parcial no hay refund.
- **Caché no cobra**: lectura profunda premium servida desde `reading_cache` (clave con prefijo `premium:`) no descuenta crédito; se descuenta solo al generar (cache miss).
- **i18n paridad es=en**: toda key nueva va en `apps/web/messages/es.json` Y `en.json` con las MISMAS keys (lo exige `apps/web/app/__tests__/i18n.test.tsx`). El español lo escribe el implementador; voz de Aluna: cálida, de vos, sin tecnicismos.
- **Config por env con defaults**: `ALUNA_CREDIT_COST_CHAT=1`, `ALUNA_CREDIT_COST_READING=3`, `ALUNA_PLUS_MONTHLY_CREDITS=60`, `ALUNA_FREE_DAILY_CHAT_CAP=5`, `ALUNA_PREMIUM_MODEL=claude-sonnet-5`.
- **NO tocar** `resolveReportCascade` (informes largos) ni el flujo de referidos del webhook: solo AGREGAR.
- Los tests existentes de `app/api/area-reading/__tests__/route.test.ts` pueden estar rotos de antes (sesión paralela) — no son de este plan; no "arreglarlos" ni romperlos más.
- Commits frecuentes, mensajes `feat(creditos): …` / `test(creditos): …`.

---

### Task 1: Migración 0022 + tipos de base de datos

**Files:**
- Create: `supabase/migrations/0022_creditos.sql`
- Modify: `packages/supabase/src/database.types.ts` (agregar tablas/funciones nuevas siguiendo el formato existente del archivo)

**Interfaces:**
- Produces: tablas `credit_ledger`, `usage_daily`; RPCs `spend_credits(p_user, p_amount, p_ref) → boolean`, `grant_credits(p_user, p_amount, p_kind, p_ref) → boolean`, `my_credit_balance() → integer`, `bump_chat_usage(p_user) → integer`. Tasks 2–7 llaman estos RPCs por nombre EXACTO.

- [ ] **Step 1: Escribir la migración completa**

```sql
-- 0022: sistema de créditos — ledger append-only (saldo = sum(delta)) +
-- funciones atómicas. Solo service_role escribe (mismo criterio que
-- subscriptions, 0005); el usuario lee su propio historial.

create table public.credit_ledger (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.profiles_user(id) on delete cascade,
  delta integer not null check (delta <> 0),
  kind text not null check (kind in ('purchase','refill','spend','refund','grant')),
  -- ref namespaced para idempotencia: 'dodo:<payment_id>' | 'refill:<sub>:<period>'
  -- | 'spend:<uuid>' | 'refund:<spend-ref>'
  ref text,
  created_at timestamptz not null default now()
);

alter table public.credit_ledger enable row level security;
create policy "own credit select" on public.credit_ledger
  for select using (user_id = auth.uid());

create unique index credit_ledger_ref_unique on public.credit_ledger (ref) where ref is not null;
create index credit_ledger_user_created on public.credit_ledger (user_id, created_at desc);

-- Débito atómico: lock advisory por usuario + verificación de saldo + insert
-- en la misma transacción. false = saldo insuficiente (NO lanza). El saldo
-- jamás queda negativo — garantía anti-quemada antes de llamar a la API.
create or replace function public.spend_credits(p_user uuid, p_amount integer, p_ref text)
returns boolean
language plpgsql security definer
set search_path = public
as $$
declare
  v_balance integer;
begin
  if p_amount <= 0 then
    raise exception 'spend_credits: p_amount must be > 0';
  end if;
  perform pg_advisory_xact_lock(hashtext('credits:' || p_user::text));
  select coalesce(sum(delta), 0) into v_balance
    from credit_ledger where user_id = p_user;
  if v_balance < p_amount then
    return false;
  end if;
  insert into credit_ledger (user_id, delta, kind, ref)
    values (p_user, -p_amount, 'spend', p_ref);
  return true;
exception when unique_violation then
  -- mismo ref reintentado: ya descontado una vez; no cobrar de nuevo
  return true;
end;
$$;

revoke execute on function public.spend_credits(uuid, integer, text) from anon, authenticated, public;
grant execute on function public.spend_credits(uuid, integer, text) to service_role;

-- Abono idempotente (compra, refill, refund, regalo). false = ref ya abonado.
create or replace function public.grant_credits(p_user uuid, p_amount integer, p_kind text, p_ref text)
returns boolean
language plpgsql security definer
set search_path = public
as $$
begin
  if p_amount <= 0 then
    raise exception 'grant_credits: p_amount must be > 0';
  end if;
  insert into credit_ledger (user_id, delta, kind, ref)
    values (p_user, p_amount, p_kind, p_ref);
  return true;
exception when unique_violation then
  return false;
end;
$$;

revoke execute on function public.grant_credits(uuid, integer, text, text) from anon, authenticated, public;
grant execute on function public.grant_credits(uuid, integer, text, text) to service_role;

-- Saldo del propio usuario (GET /api/credits con el cliente RLS del request).
create or replace function public.my_credit_balance()
returns integer
language sql security definer
set search_path = public
as $$
  select coalesce(sum(delta), 0)::integer
    from credit_ledger where user_id = auth.uid();
$$;

revoke execute on function public.my_credit_balance() from anon, public;
grant execute on function public.my_credit_balance() to authenticated, service_role;

-- Tope diario del nivel gratis (contador por usuario/día).
create table public.usage_daily (
  user_id uuid not null references public.profiles_user(id) on delete cascade,
  day date not null,
  chat_count integer not null default 0,
  primary key (user_id, day)
);
alter table public.usage_daily enable row level security;
create policy "own usage select" on public.usage_daily
  for select using (user_id = auth.uid());

create or replace function public.bump_chat_usage(p_user uuid)
returns integer
language plpgsql security definer
set search_path = public
as $$
declare
  v_count integer;
begin
  insert into usage_daily (user_id, day, chat_count)
    values (p_user, current_date, 1)
  on conflict (user_id, day)
    do update set chat_count = usage_daily.chat_count + 1
  returning chat_count into v_count;
  return v_count;
end;
$$;

revoke execute on function public.bump_chat_usage(uuid) from anon, authenticated, public;
grant execute on function public.bump_chat_usage(uuid) to service_role;
```

- [ ] **Step 2: Tipos** — abrir `packages/supabase/src/database.types.ts`, estudiar cómo están declaradas `subscriptions` (Row/Insert/Update) y las `Functions` existentes (p.ej. `user_id_by_email`), y agregar EXACTAMENTE en ese formato: `credit_ledger`, `usage_daily`, y las 4 funciones con sus Args/Returns.

- [ ] **Step 3: Verificar** — `pnpm --filter @aluna/supabase run typecheck` (si el paquete no tiene script typecheck, `pnpm --filter @aluna/web run typecheck` que lo consume). Expected: sin errores.

- [ ] **Step 4: Commit** — `git add supabase/migrations/0022_creditos.sql packages/supabase/src/database.types.ts && git commit -m "feat(creditos): migración 0022 — ledger + funciones atómicas"`

---

### Task 2: `lib/credits` — config + helpers de servidor (TDD)

**Files:**
- Create: `apps/web/lib/credits/config.ts`
- Create: `apps/web/lib/credits/ledger.ts`
- Test: `apps/web/lib/credits/__tests__/config.test.ts`, `apps/web/lib/credits/__tests__/ledger.test.ts`

**Interfaces:**
- Consumes: RPCs de Task 1.
- Produces (los usan Tasks 4–7 con estas firmas EXACTAS):

```ts
// config.ts
export const CHAT_PREMIUM_COST: number;        // env ALUNA_CREDIT_COST_CHAT, default 1  (leer lazy con función, no en import — ver nota)
export const READING_PREMIUM_COST: number;     // ALUNA_CREDIT_COST_READING, default 3
export const MONTHLY_REFILL_CREDITS: number;   // ALUNA_PLUS_MONTHLY_CREDITS, default 60
export const FREE_DAILY_CHAT_CAP: number;      // ALUNA_FREE_DAILY_CHAT_CAP, default 5
export interface CreditPack { id: "pack100" | "pack300" | "pack1000"; credits: number; productEnv: string; }
export const CREDIT_PACKS: readonly CreditPack[]; // 100/300/1000 → DODO_PRODUCT_CREDITS_100|300|1000
export function packById(id: string): CreditPack | null;
export function packProductId(pack: CreditPack): string | null; // process.env[pack.productEnv] ?? null
export function packByProductId(productId: string): CreditPack | null; // busca el pack cuyo env === productId

// ledger.ts — todos toman el client como parámetro (inyectable para tests)
import type { SupabaseClient } from "@supabase/supabase-js";
export function getCreditsServiceClient(): SupabaseClient | null; // createServiceSupabaseClient si hay URL+SERVICE_ROLE_KEY; si no null (premium apagado)
export async function spendCredits(svc: SupabaseClient, userId: string, amount: number, ref: string): Promise<boolean>; // rpc spend_credits; error de red → false (fail-closed)
export async function grantCredits(svc: SupabaseClient, userId: string, amount: number, kind: "purchase"|"refill"|"refund"|"grant", ref: string): Promise<boolean>; // rpc grant_credits; error → false
export async function refundSpend(svc: SupabaseClient, userId: string, amount: number, spendRef: string): Promise<boolean>; // grantCredits(..., "refund", `refund:${spendRef}`)
export async function bumpChatUsage(svc: SupabaseClient, userId: string): Promise<number | null>; // rpc; error → null (fail-open, el llamador no bloquea)
```

Nota config: los "const" numéricos deben leerse del env EN EL MOMENTO (funciones o getters), porque los tests van a mutar `process.env` — implementarlos como `export function chatPremiumCost(): number { ... }` etc. está PERMITIDO y es preferible; si se elige eso, actualizar los nombres aquí y avisar en el reporte (los tasks siguientes usan lo que declare el reporte).

- [ ] **Step 1: Tests primero** — casos mínimos: defaults sin env; override por env (`ALUNA_CREDIT_COST_CHAT=2` → 2); `packById("pack300").credits === 300`; `packByProductId` con env seteado y sin él; `spendCredits` true/false según respuesta mockeada del rpc (`{ data: true, error: null }` / `{ data: false, error: null }` / `{ data: null, error: {...} }` → false); `refundSpend` arma `refund:<ref>`; `bumpChatUsage` error → null. Mock del client: objeto `{ rpc: vi.fn() }`.
- [ ] **Step 2: Correr y ver fallar** — `pnpm --filter @aluna/web exec vitest run lib/credits`
- [ ] **Step 3: Implementar** ambos archivos (getCreditsServiceClient importa `createServiceSupabaseClient` desde `@aluna/supabase/server`, mismas env vars que `apps/web/app/api/chart-reading/route.ts:79-89`).
- [ ] **Step 4: Verde + commit** — `git commit -m "feat(creditos): config + helpers de ledger con débito fail-closed"`

---

### Task 3: Proveedor premium + helper compartido isRequesterPlus (TDD)

**Files:**
- Modify: `apps/web/lib/reading/provider.ts` (agregar `resolvePremiumProvider`, NO tocar lo existente)
- Create: `apps/web/lib/billing/requester-plus.ts`
- Test: `apps/web/lib/reading/__tests__/premium-provider.test.ts`, `apps/web/lib/billing/__tests__/requester-plus.test.ts`

**Interfaces:**
- Produces:

```ts
// provider.ts (agregar)
export function resolvePremiumProvider(): ResolvedProvider;
// Sin ANTHROPIC_API_KEY → { available: false }. Con llave → provider Anthropic
// con model = process.env.ALUNA_PREMIUM_MODEL || "claude-sonnet-5", reusando
// la factory anthropic YA EXISTENTE en el archivo (buscarla y reusarla tal
// cual — no duplicar la construcción del SDK).

// requester-plus.ts — extrae el patrón duplicado de
// app/api/tarot/readings/route.ts:36-48 y lib/reports/access.ts
import type { AlunaSupabaseClient } from "@/lib/supabase/route-auth"; // usar el tipo real que exporte route-auth
export async function isRequesterPlus(supabase: AlunaSupabaseClient, userId: string): Promise<boolean>;
// allAccessEnabled() → true directo; si no: select subscriptions
// (status, current_period_end) por user_id + isPlusActive de @aluna/core.
```

- [ ] **Step 1: Tests primero** — premium: sin `ANTHROPIC_API_KEY` → `available:false`; con llave → `provider.model === "claude-sonnet-5"`; con `ALUNA_PREMIUM_MODEL=claude-opus-4-8` → ese. requester-plus: all-access on → true sin tocar DB; all-access off + fila active → true; sin fila → false (mock del client con `from().select().eq().maybeSingle()` encadenado, copiar el estilo de mocks de `app/api/tarot/readings/__tests__` si existe).
- [ ] **Step 2: Fallar → implementar → verde** — `pnpm --filter @aluna/web exec vitest run lib/reading/__tests__/premium-provider lib/billing`
- [ ] **Step 3: Refactor de los 2 llamadores existentes** (`app/api/tarot/readings/route.ts`, `lib/reports/access.ts`) para usar el helper NUEVO — sus tests existentes deben seguir verdes: `pnpm --filter @aluna/web exec vitest run app/api/tarot lib/reports`
- [ ] **Step 4: Commit** — `git commit -m "feat(creditos): resolvePremiumProvider + isRequesterPlus compartido"`

---

### Task 4: `GET /api/credits` (TDD)

**Files:**
- Create: `apps/web/app/api/credits/route.ts`
- Test: `apps/web/app/api/credits/__tests__/route.test.ts`

**Interfaces:**
- Consumes: `authenticateRoute` (`@/lib/supabase/route-auth`), RPC `my_credit_balance`, select `credit_ledger`.
- Produces: `GET → 200 { balance: number, ledger: Array<{ delta, kind, created_at }> }` (últimos 20, orden desc) | `401 { error: "unauthorized" }`.

- [ ] **Step 1: Test primero** — 401 sin user; 200 con user: balance del rpc mockeado y ledger de select mockeado; si el rpc falla → `balance: 0` (no 500). Copiar el patrón de mocks de `app/api/quick-questions/__tests__` o `app/api/chat/__tests__` (mock de `authenticateRoute` vía `vi.mock`).
- [ ] **Step 2: Implementar** — `export const runtime = "nodejs"`; `authenticateRoute` → sin user 401; `supabase.rpc("my_credit_balance")` + `supabase.from("credit_ledger").select("delta, kind, created_at").order("created_at", { ascending: false }).limit(20)` (el client del request respeta RLS → solo lo suyo).
- [ ] **Step 3: Verde + commit** — `git commit -m "feat(creditos): GET /api/credits — saldo + historial"`

---

### Task 5: Chat premium + tope diario gratis (TDD)

**Files:**
- Modify: `apps/web/app/api/chat/route.ts`
- Test: `apps/web/app/api/chat/__tests__/` (agregar `premium.test.ts`; NO tocar los tests existentes)

**Interfaces:**
- Consumes: Task 2 (`getCreditsServiceClient`, `spendCredits`, `refundSpend`, `bumpChatUsage`, costo chat), Task 3 (`resolvePremiumProvider`, `isRequesterPlus`).
- Produces (contrato con la UI, Task 8): body acepta `premium?: boolean`; respuesta streaming agrega header **`x-aluna-premium: "used" | "fallback" | "off"`**; tope diario → **`429 { error: "daily_cap", cap: number }`**.

Comportamiento EXACTO (insertar tras la autenticación y la carga del perfil, antes de resolver proveedor — hoy `resolveReadingProvider` se llama con el override):

```ts
// --- créditos premium (spec 2026-07-23) ---
let premiumState: "used" | "fallback" | "off" = "off";
let premiumSpendRef: string | null = null;
let chosen = resolveReadingProvider(modelOverride);          // camino de siempre
const svc = getCreditsServiceClient();

// Tope diario del nivel gratis (solo con candados activos; fail-open sin svc)
if (!allAccessEnabled() && svc) {
  const plus = await isRequesterPlus(supabase, user.id);
  if (!plus) {
    const count = await bumpChatUsage(svc, user.id);
    if (count !== null && count > FREE_DAILY_CHAT_CAP) {
      return NextResponse.json({ error: "daily_cap", cap: FREE_DAILY_CHAT_CAP }, { status: 429 });
    }
  }
}

if (body.premium === true) {
  const prem = resolvePremiumProvider();
  if (svc && prem.available) {
    const ref = `spend:${crypto.randomUUID()}`;
    const ok = await spendCredits(svc, user.id, CHAT_PREMIUM_COST, ref);
    if (ok) { chosen = prem; premiumState = "used"; premiumSpendRef = ref; }
    else premiumState = "fallback";                          // sin saldo → Gemini/cascada
  }
  // sin svc o sin llave premium → "off": el camino gratis de siempre
}
```

En el stream: contar caracteres emitidos; en el `catch` del stream, si `premiumState === "used"` y emitidos === 0 → `await refundSpend(svc, user.id, CHAT_PREMIUM_COST, premiumSpendRef)` (best-effort, `.catch(()=>{})`). Agregar `"x-aluna-premium": premiumState` junto a los headers existentes (`x-aluna-model`, route.ts:207-214).

- [ ] **Step 1: Tests primero** (mocks como los existentes de la ruta): (a) `premium:true` con saldo → provider premium usado + header `used` + spend llamado ANTES que el provider; (b) `premium:true` sin saldo (rpc false) → cascada normal + header `fallback` + provider premium NO llamado; (c) `premium:true` sin `ANTHROPIC_API_KEY` → header `off`; (d) sin `premium` → header `off`, cero llamadas a rpcs de gasto; (e) fallo total del stream premium → refund con `refund:spend:<uuid>`; (f) all-access OFF + no-plus + count > cap → 429 `daily_cap`; (g) all-access ON → nunca bumpea.
- [ ] **Step 2: Fallar → implementar → verde** — `pnpm --filter @aluna/web exec vitest run app/api/chat`
- [ ] **Step 3: Commit** — `git commit -m "feat(creditos): chat premium con débito atómico + tope diario gratis"`

---

### Task 6: Lecturas profundas premium — helper + 4 rutas (TDD)

**Files:**
- Create: `apps/web/lib/credits/premium-reading.ts`
- Modify: `apps/web/app/api/chart-reading/route.ts`, `apps/web/app/api/area-reading/route.ts`, `apps/web/app/api/bazi-reading/route.ts`, `apps/web/app/api/horoscope-reading/route.ts`
- Test: `apps/web/lib/credits/__tests__/premium-reading.test.ts` + 1 test de integración por ruta (agregar al archivo de tests de cada ruta si existe, o crear `premium.test.ts` junto a él)

**Interfaces:**
- Consumes: Tasks 2 y 3.
- Produces:

```ts
// premium-reading.ts
import type { NextRequest } from "next/server";
export interface PremiumReading {
  mode: "free" | "premium";
  provider: ResolvedProvider;          // premium o el resuelto normal
  headerValue: "used" | "fallback" | "off";
  refundIfEmpty: () => Promise<void>;  // no-op en modo free
}
export async function resolvePremiumReading(request: NextRequest, premiumFlag: unknown, fallback: ResolvedProvider): Promise<PremiumReading>;
// premiumFlag !== true → free/off. authenticateRoute sin user → free/off (las
// rutas de lectura son públicas; premium exige sesión). Sin svc o sin llave →
// free/off. spendCredits(READING_PREMIUM_COST, `spend:<uuid>`) false → free/
// "fallback". true → premium/"used" con refundIfEmpty real.
```

Integración por ruta (patrón, usando chart-reading como referencia — las otras 3 son análogas, adaptar a su forma de caché/stream):
1. Leer `premium` del body.
2. **Caché primero**: si `premium === true`, la clave de caché lleva prefijo `premium:`; cache HIT (de cualquiera de las dos claves pedidas) → servir SIN gastar.
3. Cache miss → `resolvePremiumReading(request, body.premium, resolveReadingProvider())` → usar `pr.provider`; al cerrar el stream con contenido → `cache.set` con la clave (premium o normal); si falló con 0 chars → `pr.refundIfEmpty()`.
4. Header `x-aluna-premium: pr.headerValue` en la respuesta.

- [ ] **Step 1: Tests del helper primero** — flag false → off; sin user → off; sin saldo → fallback (y provider === fallback); con saldo → used + gasto de `READING_PREMIUM_COST`; `refundIfEmpty` abona refund. Mocks de `authenticateRoute` y rpcs como en Tasks 4–5.
- [ ] **Step 2: Implementar helper → verde.**
- [ ] **Step 3: Integrar las 4 rutas** una por una, corriendo los tests existentes de cada una tras tocarla (`vitest run app/api/chart-reading` etc.). En area-reading: 2 tests pueden estar rotos DESDE ANTES (`x-aluna-model: undefined/...`) — verificar con `git stash` si hace falta y no intentar arreglarlos.
- [ ] **Step 4: 1 test de integración por ruta** — `premium:true` con saldo usa provider premium y pone header; cache hit no gasta.
- [ ] **Step 5: Commit** — `git commit -m "feat(creditos): lecturas profundas premium (4 rutas, caché no cobra)"`

---

### Task 7: Dodo — refill mensual + packs + .env.example (TDD)

**Files:**
- Modify: `apps/web/app/api/webhooks/dodo/route.ts`, `apps/web/app/api/billing/checkout/route.ts`, `apps/web/lib/billing/dodo-client.ts`, `.env.example`
- Test: agregar casos a los tests existentes del webhook/checkout (buscarlos; si no existen, crear `__tests__/credits.test.ts` junto a cada ruta)

**Interfaces:**
- Consumes: Task 2 (`grantCredits`, `MONTHLY_REFILL_CREDITS`, `CREDIT_PACKS`, `packById`, `packProductId`, `packByProductId`).
- Produces: checkout acepta body `{ pack: "pack100" | "pack300" | "pack1000" }` (además del `{ plan }` actual) → `{ checkoutUrl }`.

Webhook (route.ts, DESPUÉS de la verificación de firma existente, sin tocar referidos):
- En eventos `subscription.active` / `subscription.renewed` (los que ya mapean a `active`), tras el upsert de `subscriptions` exitoso: `grantCredits(svc, userId, MONTHLY_REFILL_CREDITS, "refill", `refill:${dodo_subscription_id}:${current_period_end ?? "first"}`)` — idempotente por ref; best-effort (`catch` → log, no romper el 200 del webhook).
- En `payment.succeeded` (rama existente de referidos, línea ~112): además, inspeccionar el payload por productos comprados (misma forma que usa el flujo de checkout: `product_cart`/line items — el implementador debe mirar cómo llega en el payload real de `payment.succeeded`, hay ejemplos en los tests del webhook o en `lib/billing/referral-webhook.ts`); si algún `product_id` coincide con `packByProductId` → resolver `user_id` (mismo mecanismo del webhook: por suscripción o `user_id_by_email`) y `grantCredits(svc, userId, pack.credits, "purchase", `dodo:${payment_id}`)`.

Checkout (route.ts): si body trae `pack` válido → `product_cart: [{ product_id: packProductId(pack), quantity: 1 }]` SIN `subscription_data` (compra one-time), sin el guard de `already_subscribed` (los packs se pueden comprar siempre), mismo `return_url` con `?checkout=credits`. `packProductId` null → 500 `{ error: "pack_not_configured" }`. Body con `plan` → camino actual intacto.

`.env.example`: documentar (comentadas) las vars Dodo que el código ya usa y las nuevas: `DODO_PAYMENTS_API_KEY`, `DODO_PAYMENTS_WEBHOOK_SECRET`, `DODO_PRODUCT_MONTHLY`, `DODO_PRODUCT_YEARLY`, `DODO_PRODUCT_CREDITS_100|300|1000`, `NEXT_PUBLIC_APP_URL`, `ALUNA_ALL_ACCESS`, `ALUNA_PREMIUM_MODEL`, `ALUNA_CREDIT_COST_CHAT`, `ALUNA_CREDIT_COST_READING`, `ALUNA_PLUS_MONTHLY_CREDITS`, `ALUNA_FREE_DAILY_CHAT_CAP` — con 1 línea de comentario cada una en el estilo del archivo.

- [ ] **Step 1: Tests primero** — webhook: `subscription.renewed` abona refill idempotente (2ª entrega del mismo evento NO duplica: `grant_credits` devuelve false y el webhook sigue 200); `payment.succeeded` con product de pack abona `dodo:<payment_id>`; `payment.succeeded` de suscripción normal NO abona pack. checkout: `{pack:"pack100"}` → cart correcto sin trial; pack sin env → 500; `{plan:"monthly"}` sigue igual (test existente verde).
- [ ] **Step 2: Fallar → implementar → verde** — `vitest run app/api/webhooks app/api/billing`
- [ ] **Step 3: Commit** — `git commit -m "feat(creditos): refill mensual + packs por Dodo, idempotente"`

---

### Task 8: UI chat — toggle ✨, chip de saldo, banners (i18n es+en)

**Files:**
- Modify: `apps/web/app/(app)/preguntar/chat-view.tsx`, `apps/web/app/(app)/preguntar/chat.module.css`, `apps/web/messages/es.json`, `apps/web/messages/en.json`
- Create: `apps/web/lib/credits/premium-client.ts`
- Test: `apps/web/app/(app)/preguntar/__tests__/premium-toggle.test.tsx` (+ los tests existentes de chat-view siguen verdes)

**Interfaces:**
- Consumes: contrato de Task 5 (body `premium`, header `x-aluna-premium`, 429 `daily_cap`) y `GET /api/credits`.
- Produces: `premium-client.ts`:

```ts
export function getPremiumEnabled(): boolean;              // localStorage "aluna:premium" === "1"; SSR-safe (false)
export function setPremiumEnabled(on: boolean): void;
```

Comportamiento:
- Botón toggle **✨** en el composer (junto al input, antes del botón enviar; `aria-pressed`, title con i18n). Encendido → `sendText` manda `premium: true`.
- **Chip de saldo** (`✨ N`) visible cuando el toggle está encendido: fetch a `/api/credits` al montar y tras cada respuesta con header `x-aluna-premium: used`; si `!res.ok` (sin sesión) → no mostrar chip.
- Tras una respuesta con header `fallback` → línea de aviso bajo el hilo (estilo `styles.thinking` existente): `t("premiumOut")` con link a `/ajustes`.
- Respuesta `429 daily_cap` → aviso `t("dailyCap", { cap })` (hoy inalcanzable con all-access, pero el manejo queda listo).
- Keys nuevas (MISMAS en `es.json` y `en.json`, namespace `chat`): `premiumToggle`, `premiumOut`, `dailyCap`, `creditsChip` (usar `{n}`/`{cap}` como parámetros). Español: voz cálida de Aluna, de vos ("Te quedaste sin créditos premium — conseguí más en Ajustes").
- El toggle NO aparece dentro de `embedded` si el espacio no da — SÍ aparece: el chat embebido de las lentes usa el mismo composer; mantenerlo compacto (solo ícono).

- [ ] **Step 1: Test del toggle** — render, click → `aria-pressed=true` y persistencia en localStorage; `sendText` con toggle on incluye `premium: true` en el body del fetch (mock de fetch); respuesta con header fallback muestra `premiumOut`. Seguir el estilo de los tests existentes de chat-view.
- [ ] **Step 2: Implementar → verde** — `vitest run "app/(app)/preguntar" app/__tests__/i18n.test.tsx`
- [ ] **Step 3: Commit** — `git commit -m "feat(creditos): toggle premium + saldo + avisos en el chat"`

---

### Task 9: UI Ajustes — sección Créditos (i18n es+en)

**Files:**
- Create: `apps/web/app/(app)/ajustes/credits-card.tsx`
- Modify: `apps/web/app/(app)/ajustes/page.tsx` (renderizar bajo `PlanCard`), `apps/web/app/(app)/ajustes/settings.module.css` (si hace falta), `apps/web/messages/es.json`, `apps/web/messages/en.json`
- Test: `apps/web/app/(app)/ajustes/__tests__/credits-card.test.tsx`

**Interfaces:**
- Consumes: `GET /api/credits`, `POST /api/billing/checkout` con `{ pack }` (Task 7), `CREDIT_PACKS` (Task 2, importable — es isomórfico: no usa llaves).

Comportamiento (client component, mismo patrón visual/estados que `plan-card.tsx` — leerlo primero):
- Saldo grande (`✨ {balance}`) + subtítulo i18n.
- 3 botones de pack (`credits.pack100` etc. — el precio NO se muestra, lo muestra el checkout de Dodo) → `fetch("/api/billing/checkout", { body: { pack } })` → `window.location.href = checkoutUrl`; estados loading/error como en plan-card.
- Historial: últimos 10 movimientos del ledger (`+60 refill · 12 jul`), formato con `Intl.DateTimeFormat(locale)`.
- Sin sesión/fetch falla → card con `credits.error`.
- Namespace i18n nuevo `credits` en es+en: `title`, `balanceLabel`, `buyTitle`, `pack100`, `pack300`, `pack1000`, `historyTitle`, `empty`, `error`, `loading`, y labels de kind: `kindPurchase`, `kindRefill`, `kindSpend`, `kindRefund`, `kindGrant`.
- `?checkout=credits` en la URL (vuelta del checkout) → refetch del saldo + mensajito `credits.purchaseThanks`.

- [ ] **Step 1: Test** — render con fetch mockeado: saldo visible, 3 packs, click pack → POST con `{pack:"pack100"}`; fetch fallido → error card.
- [ ] **Step 2: Implementar → verde** — `vitest run "app/(app)/ajustes" app/__tests__/i18n.test.tsx`
- [ ] **Step 3: Commit** — `git commit -m "feat(creditos): sección Créditos en Ajustes — saldo, packs, historial"`

---

### Task 10 (final, la corre el controlador): review Fable de rama completa + suite completa + merge

No es un task de implementador. Checklist del controlador: suite completa web + core, review Fable 5 con el paquete de rama entero, UN fixer para todos los hallazgos, re-review del fix, merge a main vía worktree efímero, avisar a Gio: aplicar migración 0022 en Supabase + crear los 3 productos de créditos en Dodo + setear envs.
